# Composite action: Docker build + push to Docker Hub
# Equivalent of azure-pipelines/templates/docker-build-push.yml
name: Docker Build & Push to Docker Hub
description: Log in to Docker Hub, build the service image, push with run-id and latest-<branch> tags.

inputs:
  service-name:
    description: Service name — used as the image repository name on Docker Hub
    required: true
  service-dir:
    description: Path to the service directory containing the Dockerfile
    required: true
  dockerhub-username:
    description: Docker Hub username
    required: true
  dockerhub-token:
    description: Docker Hub personal access token
    required: true

runs:
  using: composite
  steps:
    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ inputs.dockerhub-username }}
        password: ${{ inputs.dockerhub-token }}

    - name: Build and push ${{ inputs.service-name }}
      uses: docker/build-push-action@v5
      with:
        context: ${{ inputs.service-dir }}
        file: ${{ inputs.service-dir }}/Dockerfile
        push: true
        tags: |
          ${{ inputs.dockerhub-username }}/${{ inputs.service-name }}:${{ github.run_id }}
          ${{ inputs.dockerhub-username }}/${{ inputs.service-name }}:latest-${{ github.ref_name }}
